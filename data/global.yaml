### Global settings
timezone: UTC

# Users/groups to be created and managed
base_users:
  - "orbit"

# SSH keys to be added from GitHub
github_user: "sloraris"
ssh_user: "orbit"

# Docker configuration
docker::manage_users: true
docker::docker_users:
  - "orbit"

# Docker Swarm settings
docker_swarm_manager: "luna.orbit"
docker_swarm_manager_ip: "10.0.3.11"

# Komodo node settings
komodo_user: "komodo"
komodo_group: "komodo"
core_node: "luna.orbit"
